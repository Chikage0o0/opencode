## 为什么

<!-- 说明本次变更的动机。解决什么问题？为何现在做？
     路径、命令、代码标识符、专有名词保持原样。 -->

## 变更内容

<!-- 描述将要改变什么。具体说明新增能力、修改或移除项。
     破坏性变更用 **BREAKING** 标记。 -->

## 能力

### 新能力
<!-- 本次引入的能力。将 <name> 替换为 kebab-case 标识符（例如 user-auth、data-export、api-rate-limiting）。
     每个能力会生成 specs/<name>/spec.md，内含 OpenSpec delta 标题与 Gherkin 风格场景。
     能力标识符（kebab-case）保持英文。 -->
- `<name>`: <该能力覆盖范围的简要说明>

### 修改的能力
<!-- 行为正在变化的既有能力（不仅是实现细节）。
     仅在规格层行为变化时列出。每个需要对应的 delta spec.md。
     使用 openspec/specs/ 中已有规格名。若无需求变更则留空。
     能力标识符保持英文。 -->
- `<existing-name>`: <行为如何变化的说明>

## 影响范围

<!-- 受影响的代码、API、依赖或系统。路径、API 名、依赖名保持原样。 -->
