interface:
  display_name: "Resolving Merge Conflicts"
  short_description: "Resolve Git conflicts without losing intent"
  default_prompt: "Use $resolving-merge-conflicts to analyze and safely resolve the current Git conflicts."
